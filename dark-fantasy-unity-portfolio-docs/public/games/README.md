# WebGL Game Builds

Phase 3 expects Unity WebGL builds here.

Recommended structure:

```txt
public/games/
  biome-gate/
    index.html
    Build/
    TemplateData/
```

Current status:

- `biome-gate/index.html` is a lightweight browser demo placeholder so the Try Now modal can be tested.
- Replace the folder contents with a real Unity WebGL export when available.
